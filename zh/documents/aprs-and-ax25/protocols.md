---
order: "3.1"
title: 协议
---

# {{$frontmatter.order}} {{ $frontmatter.title }}

在链路层，APRS 使用 AX.25 协议（如《业余分组无线链路层协议》中所定义，详见附录 6），仅使用无编号信息（UI）帧。这意味着 APRS 以无连接模式运行，即 AX.25 帧在不期望任何响应的情况下传输，另一端的接收也不保证。

在更高层级，APRS 支持消息协议，允许用户向指定电台发送短消息（一行文本），并期望从这些电台收到确认。