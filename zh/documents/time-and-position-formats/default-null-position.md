---
order: "6.7"
title: 默认空位置
---

# {{$frontmatter.order}} {{ $frontmatter.title }}

当电台没有特定的位置信息要传输时（例如，未连接 GPS 接收机的 Mic-E 单元），电台必须在位置字段中传输默认空位置。

空位置对应于北纬 0° 0' 0"，西经 0° 0' 0"。

空位置应包含 `\.` 符号（未知/不确定位置）。例如，位置未知的电台的位置报告将包含以下坐标：

```
...0000.00N\00000.00W....
```
