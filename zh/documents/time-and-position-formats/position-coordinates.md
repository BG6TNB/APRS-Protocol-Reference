---
order: "6.5"
title: 位置坐标
---

# {{$frontmatter.order}} {{ $frontmatter.title }}

位置坐标由纬度和经度组合而成，用显示符号表标识符分隔，后跟符号代码。例如：

```
4903.50N/07201.75W-
```

纬度和经度之间的 `/` 字符是符号表标识符（此处表示使用主符号表），末尾的 `-` 字符是该表中的符号代码（此处表示"房屋"图标）。

显示符号的描述位于第 20 章：APRS 符号。完整的符号表列表见附录 2。
