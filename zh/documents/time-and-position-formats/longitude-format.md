---
order: "6.4"
title: 经度格式
---

# {{$frontmatter.order}} {{ $frontmatter.title }}

经度以固定的 9 字符字段表示，单位为度和十进制分（精确到两位小数），后跟字母 E（东经）或 W（西经）。

经度的度范围为 000 到 180。经度的分由整数分和分的百分之一组成，以小数点分隔。

例如：

```
07201.75W 表示西经 72 度 1 分 45 秒。
```

在通用格式示例中，经度显示为 9 字符字符串 dddmm.hhW（即度、分和分的百分之一，西经）。
