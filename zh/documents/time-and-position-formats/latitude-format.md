---
order: "6.3"
title: 纬度格式
---

# {{$frontmatter.order}} {{ $frontmatter.title }}

纬度以固定的 8 字符字段表示，单位为度和十进制分（精确到两位小数），后跟字母 N（北纬）或 S（南纬）。

纬度的度范围为 00 到 90。纬度的分由整数分和分的百分之一组成，以小数点分隔。

例如：

```
4903.50N 表示北纬 49 度 3 分 30 秒。
```

在通用格式示例中，纬度显示为 8 字符字符串 ddmm.hhN（即度、分和分的百分之一，北纬）。
