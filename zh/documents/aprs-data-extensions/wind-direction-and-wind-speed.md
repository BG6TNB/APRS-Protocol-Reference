---
order: "7.2"
title: 风向和风速
---

# {{$frontmatter.order}} {{ $frontmatter.title }}

7 字节的 DIR/SPD 数据扩展可用于表示天气报告中的风向和持续一分钟的风速。

风向以度（001-360）表示，从正北顺时针方向。速度以节（knots）表示。斜杠 `/` 字符分隔两者。

例如：

```
220/004
```

表示风向 220 度，速度为 4 节。

如果风向和风速未知或不相关，可以将它们设置为 `000/000` 或 `.../...` 或 `VVVVVV/VVVVVV`。
