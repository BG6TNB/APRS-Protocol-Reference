---
order: "4.5"
title: Mic-E 编码数据
---

# {{$frontmatter.order}} {{ $frontmatter.title }}

AX.25 目的地址字段的另一种替代用途是包含 Mic-E 编码数据。这些数据包括：

- 电台的纬度。
- 西/东指示器和经度偏移指示器（用于经度计算）。
- 消息代码。
- APRS 数字中继器路径。

这些数据与 AX.25 信息字段中的相关数据一起使用，以提供完整的位置报告和有关电台的其他信息（参见第 10 章：Mic-E 数据格式）。