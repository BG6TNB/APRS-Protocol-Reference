---
order: "4.1"
title: AX.25 目的地址字段
---

# {{$frontmatter.order}} {{ $frontmatter.title }}

AX.25 目的地址字段可包含 6 种不同类型的 APRS 信息：

- 通用 APRS 地址。
- 带符号的通用 APRS 地址。
- APRS 软件版本号。
- Mic-E 编码数据。
- Maidenhead 网格定位器（已淘汰）。
- 备用网络（ALTNET）地址。

在所有这些情况下，目的地址 SSID 可用于指定通用 APRS 数字中继器路径。