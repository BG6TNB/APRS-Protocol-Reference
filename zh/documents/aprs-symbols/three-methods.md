---
order: "20.1"
title: 三种方法
---

# {{$frontmatter.order}} {{ $frontmatter.title }}

指定 APRS 符号（显示图标）有三种方法：

- 在 AX.25 信息字段中。
- 在 AX.25 目标地址中。
- 在 AX.25 源地址的 SSID 中。

首选方法是在信息字段中包含符号。但是，如果无法这样做（例如，在独立跟踪器中无法将符号引入信息字段），则可以使用其他两种方法中的任意一种。