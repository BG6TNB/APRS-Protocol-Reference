---
order: "20.2"
title: 符号表
---

# {{$frontmatter.order}} {{ $frontmatter.title }}

有两个 APRS 符号表：

- **主符号表**
- **备用符号表**

主符号表和备用符号表之间的本质区别在于，备用符号表中的某些符号可以叠加字母数字字符。例如，备用符号表中的 "汽车" 图标可以叠加数字 "3"，表示它是 3 号车。能够叠加的符号标记为 [可叠加]。主符号表中的所有符号都不能叠加。

有关这些表的完整列表，请参见附录 2。