---
order: "10.24"
title: 非 APRS 网络中的 Mic-E 数据
---

# {{$frontmatter.order}} {{ $frontmatter.title }}

Mic-E AX.25 信息字段的某些部分可能包含二进制数据（即非打印 ASCII 字符）。如果此类数据包在 APRS 网络中传输，这不会造成任何困难。

但是，如果数据包要通过不能可靠保持二进制数据的网络（例如互联网）转发，则需要将数据转换为能够保持其格式的形式。

此外，如果数据包随后重新回到 APRS 网络，则需要将数据重新转换回其原始格式。
