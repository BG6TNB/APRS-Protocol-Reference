---
order: "9.9"
title: 新型跟踪器
---

# {{$frontmatter.order}} {{ $frontmatter.title }}

跟踪器固件可以将 GPS 数据直接压缩为 APRS 压缩格式。它们将使用 `!` 数据类型指示器，表示位置是实时的，且跟踪器不具备 APRS 功能。

如果位置报告不是实时的，则可以使用 `/` 数据类型指示器替代，以便包含最新的定位时间。