---
order: "9.10"
title: 旧式跟踪器
---

# {{$frontmatter.order}} {{ $frontmatter.title }}

一些数字中继器能够将来自现有跟踪器的原始 NMEA 字符串转换为压缩数据格式，以便进一步转发。

如果跟踪器的目标地址为 `GPS`（注意：这是 3 个字母的地址 `GPS`，而非 `GPS*`），这些数字中继器将压缩数据。

不希望其数据包被 APRS 网络修改的跟踪器将使用其他有效的通用 APRS 目标地址。